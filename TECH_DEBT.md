# QAYD — Tech Debt Log

Per MANIFEST Law 5: every "we'll fix it later" is recorded here (or as a tracked issue), never left to
memory. Each item names its source story, severity, the decision taken, and the planned resolution.
Items are removed only when resolved (with the resolving commit noted).

| ID | Source | Severity | Item | Planned resolution |
|---|---|---|---|---|
| TD-01 | S1-05 | Medium | Raw query-builder / raw SQL (`DB::table(...)`, `DB::select(...)`) on the **default/owner** Postgres connection bypasses RLS. Tenant **Eloquent** models are safe — `BelongsToCompany` binds them to the non-superuser `pgsql_app` connection, enforced by an arch test — but a raw call on the owner connection would not be tenant-scoped. | Route any raw tenant queries through the `pgsql_app` connection; add a static/arch check that flags raw `DB::` calls against tenant tables. Revisit as query-heavy stories land (S1-09+). |
| TD-04 | S1-05 | Low | `is_platform_admin` exists and sets the `app.is_platform_admin` GUC, but is **not** wired into any cross-tenant read bypass in the RLS policies (kept strict / fail-closed). The platform-diagnostics read escape hatch is deferred. | Design the platform-admin cross-tenant path later as an **explicit, narrow, audited** service-role path (never an implicit policy bypass). |
| TD-05 | S1-04 / S1-06 | Low | Schema/RLS tests run `migrate:fresh` against the real dev `qayd` database (destructive by design; fine for a foundations/CI DB), and the "migrate once" guard is per-process. | A future `pest --parallel` run needs a per-worker database; wire this into the CI DB setup (S1-02) or the test bootstrap. |
| TD-06 | S1-16 | Low | The `audit_logs` skeleton defers the full audit spec: monthly range **partitioning**, the per-company **SHA-256 hash chain** (`hash`/`prev_hash` columns are present so it can be added without a rewrite), the PL/pgSQL row-diff **shadow-table** capture, and the **outbox/queue** write path. Table + append-only + RLS + `AuditLogger::record` writer only. | Implement partitioning + hash chain + outbox as audit volume/compliance needs arise (post-Sprint-1). |
| TD-07 | S1-08 | Medium | CSRF is not wired on the **Sanctum cookie (web SPA) login flow** — the stateful cookie session lacks CSRF/419 enforcement (bearer/JWT requests are unaffected). Correct wiring needs Sanctum's origin-gated `EnsureFrontendRequestsAreStateful` + `/sanctum/csrf-cookie`, which the API test client does not exercise. | Wire CSRF for the cookie flow in the frontend auth stage (S1-15) or a hardening story, **before** the SPA cookie login ships to users. |
| TD-08 | S1-15 / S1-08 | Low | Password-reset (`/forgot-password`, `/reset-password`) and full **MFA** (TOTP/WebAuthn) are not implemented — the frontend screens exist as disabled stubs, and S1-08 stubbed the login MFA branch to the non-MFA path (both explicitly out of Sprint-1 scope per SPRINT_01). | Implement the password-reset backend + MFA in a post-Sprint-1 hardening story and wire the stubbed screens then. |
| TD-09 | S1-08 | Low | **Logout does not revoke the already-issued access token.** `LogoutController` revokes the presented refresh token and invalidates/re-keys the Sanctum session, but a bearer **JWT access token (`jti`)** stays valid until its 15-min expiry — so a token captured before logout still authenticates for up to `JWT_ACCESS_TTL`. (Refresh-token rotation-on-use with family-wide **reuse detection** IS implemented, so the refresh side is covered.) Source `app/Http/Controllers/Identity/LogoutController.php`. | Add a short-lived `jti` **denylist** (Redis, TTL = remaining access-token lifetime) checked by the JWT guard, in a post-Sprint-1 hardening story, for instant access-token revocation on logout. |
| TD-10 | S2-01 | Low | **`SetOpeningBalanceAction` is deferred** — one of the five COA actions named in SPRINT_01 §S2-01, but an opening balance must post through the posting engine (the ledger's single way in), which is built in S2-05. Implementing it in S2-01 would either fork a second write path into the ledger or fake a stored balance column, both of which violate the "one and only one way into the ledger" invariant and S2-01's own exclusions (no posting engine/ledger). The four structural COA actions (create / update / reclassify / deactivate), the schema, RLS, and the seed are complete. | **UNBLOCKED as of S2-05** — `PostingService` now exists, so the opening entry can post through the one path against an Opening-Balance-Equity account. Not implemented in S2-05 itself: the authorization for that story was an explicit "implement ONLY" list (posting engine only) that excluded it, and it is chart-of-accounts work. Schedule into the next COA-touching story. |
| TD-11 | S2-01 | Medium | The chart-of-accounts "a posted account cannot be renumbered or reclassified" guard runs against `NoLedgerPostedActivityGuard` (returns false — no ledger exists yet, bound in `AppServiceProvider`), so the rule is enforced structurally and unit-tested via a stub, but not yet against real posted lines. | **UNBLOCKED as of S2-05** — `ledger_entries` now exists and carries real posted lines, so a ledger-backed `PostedActivityGuard` (`SELECT 1 FROM ledger_entries WHERE account_id = ?`) is now implementable, along with the integration test that reclassifying / renumbering an account carrying posted lines is refused (`422 ACCOUNT_HAS_POSTINGS`). Not implemented in S2-05 itself: that story's authorization was an explicit "implement ONLY" list (posting engine only) that excluded it, and swapping the binding changes chart-of-accounts behavior. Schedule into the next COA-touching story — until then the rule stays structurally enforced but ledger-blind. |
| TD-12 | S2-04 | Low | **Accepted S2-04 domain-review improvements** (no defect — provisionally-approved design): (1) **money precision/range** is not validated before the DB write, so an amount exceeding `NUMERIC(19,4)` surfaces as a raw 500 instead of a clean 422; (2) **draft mutations are not audit-logged** (create/update/submit) — the audit trail begins only at posting; (3) **line identity is intentionally ephemeral** pre-posting — `UpdateJournalDraft` fully replaces lines (delete+re-insert), which is correct only while draft line ids are not externally referenced; (4) **AI-cannot-submit is advisory** at the action layer (caller-supplied `actorIsAi` flag), not an authorization boundary; (5) **submit-time balance policy** is implicit (submit does no balance check; posting does). | (1) Validate amount scale/range (or catch the line-write `QueryException` → 422). (2) Audit draft mutations. (3) Keep line-replacement until draft line ids become externally referenced, then switch to differential updates. (4) Enforce AI-cannot-submit in the authorization/permission layer in **S2-05** (DB `trg_no_ai_autopost` stays the posting backstop). (5) Decide + document the submit-time balance policy in **S2-05**. |

| ~~TD-13~~ | S2-05 | — | **RESOLVED in S2-07** (see Resolved). The posting gate is fiscal-YEAR granular, not fiscal-PERIOD granular. `FiscalYearCalendarResolver` resolves + locks the `fiscal_years` row covering `journal_date` and refuses a non-`open` year; month-level `fiscal_periods` (with `open/closed/locked` + `module_lock`) do not exist yet. Consequently `journal_entries.fiscal_year_id` is set at posting but `fiscal_period_id` is not (the column is absent on the header, and nullable with no FK on `ledger_entries`). This is the accepted staging from the S2-05↔S2-07 sequencing review — correct for the current schema, not a weaker promise, since no month-close mechanism exists to be bypassed. | **S2-07**: bind a `fiscal_periods`-backed `FiscalCalendarResolver` (the seam already isolates `JournalEntryPostingService`), populate `ResolvedFiscalPeriod::$fiscalPeriodId`, backfill + add the FK and NOT NULL on `ledger_entries.fiscal_period_id`, and add `fiscal_period_id` to the journal header. |
| TD-14 | S2-05 | Low | **`ledger_entries` defers the dimension/branch columns** GENERAL_LEDGER.md specifies (`branch_id`, `customer_id`, `vendor_id`, `cost_center_id`, `project_id`, `department_id`) plus the `source_module` copy — because the S2-03 `journal_lines` source does not carry them yet and their tables do not exist. Reporting that filters by dimension is therefore not yet possible off the projection. | Add each column (with its real FK) in the migration that creates the corresponding dimension table + the `journal_lines` columns that feed it, and extend `projectLines()` to copy them. Before S2-11 trial-balance filtering needs them. |
| TD-15 | S2-05 | Low | **Postable-account validation checks `status = 'active'` only.** JOURNAL_ENTRIES.md also requires `is_postable = true` (a header/summary account in the COA tree must not receive a direct posting) and the control-account rule (a line hitting AR/AP must carry `customer_id`/`vendor_id`). The `accounts` table has no `is_postable` column (S2-01 did not add one), and `journal_lines` has no `customer_id`/`vendor_id` yet, so neither rule is enforceable today. | Add `accounts.is_postable` (or derive "has children ⇒ not postable" from the tree) and enforce it in `assertPostableAccounts()`; enforce the control-account sub-ledger rule once `journal_lines` carries `customer_id`/`vendor_id` (Customers/Vendors modules). |
| TD-16 | S2-05 | Low | **Posting writes no `journal_entry_history` snapshot and no `audit_logs` row** (steps 10–11 of the JOURNAL_ENTRIES.md posting algorithm). `journal_entry_history` does not exist yet, and the S1-16 `AuditLogger` is not called from the posting path — so a post is currently traceable only through the entry's own `posted_by`/`posted_at` and its ledger rows. | Create `journal_entry_history` and write the `posted` snapshot + an `AuditLogger::record` call inside the posting transaction (pairs naturally with TD-12 #2, which asks the same for draft mutations). |
| TD-17 | S2-05 | Low | **No multi-currency rounding-plug path.** `assertBalanced` requires exact equality in BOTH the entry currency and base currency. JOURNAL_ENTRIES.md permits a base-currency discrepancy below `0.0100` to be absorbed by a plug line against a Rounding Gain/Loss account; QAYD currently rejects it as a hard imbalance. Not yet reachable in practice (`base = debit × rate` is derived by one code path at one rate), but a genuine multi-currency entry with per-line rates could hit it. | Implement the configurable rounding-plug line (company-level threshold + a designated Rounding account) when per-line exchange rates / true multi-currency entries land. |
| TD-18 | S2-06 | Low | **SPRINT_02 §S2-06 names reversal columns that do not exist.** The story text specifies `reverses_entry_id` / `reversed_by_entry_id`; the shipped S2-03 schema calls them `reversed_entry_id` (on the mirror, pointing back) and `reversal_entry_id` (on the original, pointing forward). The implementation follows the **schema**, per MANIFEST Law 1 (code over stale doc) — nothing was renamed, and no migration was written to chase the spec's names. The divergence is documentation-only, but a reader working from the sprint plan will look for columns that are not there. | Correct the two column names in `docs/execution/SPRINT_02.md` §S2-06 to match the schema, in the next docs-touching commit. Do **not** rename the columns: they are load-bearing for `uq_je_one_reversal`, `chk_je_no_self_reverse`, and the S2-03 immutability trigger. |
| TD-20 | S2-07 | Medium | **Nothing keeps a fiscal year's status in sync with its periods.** The posting gate reads the PERIOD only — deliberately, since the period is the finer authority — and period status is derived from the year exactly once, when the periods are generated. After that the two drift freely: setting `fiscal_years.status = 'closed'` by hand leaves twelve `open` periods that still accept postings into a closed year. Not reachable through any shipped code path (no action writes `fiscal_years.status` after onboarding), which is why it is debt and not a defect. | Own the coupling in the fiscal-year lifecycle story: `CloseFiscalYearAction` closes the year's periods in the same transaction, `ReopenFiscalYearAction` is refused while any period is `locked`. Consider a DB trigger refusing an `open` period under a non-`open` year, so the invariant does not depend on the Action being the only writer. |
| TD-21 | S2-07 | Low | **A fiscal year created after onboarding gets no periods.** Only two things generate them: `CreateCompanyAction` (the company's first year) and the S2-07 migration (years that already existed). There is no `CreateFiscalYearAction`, so when a company reaches the end of FY2026 there is no supported way to open FY2027 — and a year with no periods accepts no postings at all. Out of S2-07's scope (the story specifies close/lock, not year creation), but it is a wall every company hits exactly once a year. | Add `CreateFiscalYearAction` (or a year-rollover step in the year-close story) that creates the year and calls `MonthlyFiscalPeriodGenerator` in the same transaction, mirroring onboarding. Needed before the first customer's second fiscal year, not before launch. |
| TD-22 | S2-07 | Low | **`fiscal_periods.module_lock` is written by nothing.** The column ships with the table (per GENERAL_LEDGER.md) so that adding per-module close does not later require a migration on a ledger-adjacent table, but no module reads or writes it, and `ClosePeriodAction` closes a period wholesale rather than per module. | Write it from the sub-ledger close paths as Sales/Purchases/Payroll land, and extend the posting gate to consult `module_lock[source_module]` once `journal_lines` carries `source_module` (TD-14). |
| TD-23 | S2-07 | Low | **Permission-name divergence between the two accounting specs.** `accounting.period.lock` is named in JOURNAL_ENTRIES.md but is absent from the GENERAL_LEDGER.md permission table, which lists only `close`, `reopen` and `hard_lock_override`. S2-07 seeds all four (lock granted to Owner/CEO/CFO/Finance Manager), taking the union, because a lock transition with no permission of its own would have to borrow `close`'s — which would let anyone who can close a month also make it audit-final. | Reconcile the two documents in the next docs-touching commit; the seeded catalogue is the de-facto answer. |
| TD-24 | S2-08 | Low | **The GL read has no cached aggregate and no count endpoint.** GENERAL_LEDGER.md names `mv_account_running_balance` (a materialized view refreshed on a debounced schedule) as the performance path for running balances; S2-08 computes the balance per request with a window function instead. That is the right call now — a view refreshed on a schedule is *stale by design*, which is the wrong trade for a figure an accountant is reading, and `idx_ledger_account_date` makes the live query cheap at current volumes. Separately, the API standard says a caller needing an approximate total on a cursor-paginated resource uses a dedicated `/summary` or `/count` endpoint; none exists, so `meta.pagination.total` is always null. | Add the materialized view (or a period-rollup table maintained by an AFTER INSERT trigger on the append-only projection, which stays exact rather than merely fresh) when a real account's activity query is measured slow — not before. Add `/summary` when a caller actually needs the count. |
| TD-25 | S2-09 | Low | **The trial-balance module ships its narrow story, not the frozen chapter.** TRIAL_BALANCE.md specifies a much larger feature than SPRINT_02 §S2-09 asks for: `trial_balance_approvals` (an ordered multi-step approval chain), `trial_balance_ai_findings`, `trial_balance_exports`, the `adjusted`/`post_closing` types with `parent_snapshot_id` adjustment tracking, archiving, and `currency_breakdown` per line. S2-09 implements compute + snapshot + single-signature approve + the async path, which is what the story specifies. The enum values and columns for the rest are present so the later stories add behaviour rather than migrate a populated table. | Build each as its own story when the sprint plan calls for it. The single `approved_by` is the one to watch: moving to a real chain means `approved_at` becomes "when the LAST step passed", so the approval-chain story must not simply repurpose the existing column. |
| TD-26 | S2-09 | Low | **SPRINT_02's header contradicts its own story table.** The header reads "Committed: 47 pts", but the fourteen story rows sum to **69**. Verified by summing the table. Earlier close-out reports in this sprint quoted the header figure and therefore understated the denominator ("39 of 47", "42 of 47" should have read 39/69 and 42/69); PROJECT_STATUS now uses the table sum, which is the number that can actually be checked. | Correct the header in `docs/execution/SPRINT_02.md` in the next docs-touching commit; do not restate the per-story estimates, which are the reviewed ones. |
| TD-27 | S2-11 | Low | **The editor's client-side balance check is a deliberate second implementation of a server rule.** Every other story forbids duplicating backend logic; S2-11's DoD explicitly requires it ("a client-side `deriveBalance` that flags imbalance the same way the backend will"), so `apps/web/lib/accounting/balance.ts` now derives totals the posting engine also derives. Two things bound the risk: the client copy only ever DISABLES the Post button — it can never mark something postable — and a backend `balance_mismatch` is rendered verbatim, asserted by a test comparing the alert's text to the server's exact string. The residual exposure is a *tolerance* divergence: the server treats a rounding tolerance as configurable (TD-17; 0.0050 for the trial balance today), while the editor's check is exact-zero. | If a multi-currency rounding-plug path lands (TD-17), revisit whether the editor should read the company's tolerance rather than assume zero. Do NOT move the check server-side and round-trip on every keystroke — the latency would defeat the affordance it exists to provide. |
| TD-28 | S2-11 | Low | **`apps/web` has one time-sensitive test.** `create-company-wizard.test.tsx` timed out once at 5.9s against vitest's 5s default while the suite was under load, then passed on re-run; it is unrelated to S2-11 and was not modified by it. It is a real flake risk in CI as the suite grows. | Give that test an explicit longer timeout, or replace its waits with deterministic assertions. Not urgent — one observed occurrence — but it will recur. |
| TD-19 | S2-06 | Low | **No single declared status-transition map.** `journal_entries.status` now carries eight values (`draft`, `pending_approval`, `approved`, `rejected`, `posted`, `reversed`, `voided`, `archived`). Every transition is guarded — but the guards are distributed across the individual Actions (`POSTABLE_STATUSES`, `VOIDABLE_STATUSES`, `EDITABLE_STATUSES`) and the DB constraints, with no one place that declares the whole legal graph. Adding a ninth status, or a new Action, means finding every guard by hand. | Declare the transition map once (a single `const array<string, list<string>>` on the model or a small `JournalEntryLifecycle` value object) and have each Action consult it, keeping the DB constraints as the independent backstop. Do it **before** the next story that adds a status or a lifecycle Action — approval flow (S3) is the likely trigger. |

## Resolved

| ID | Source | Item | Resolution |
|---|---|---|---|
| TD-02 | S1-04 | `users.uuid` had no unique index (AUTH_SERVICE.md DDL declared only `uq_users_email`). | Resolved — a UNIQUE index `uq_users_uuid` was added on `users (uuid)` in migration `2026_07_27_000013_add_unique_index_to_users_uuid.php`, per ADR-0010's uuid-uniqueness carve-out (resolved in this commit / ADR-0010). |
| TD-13 | S2-05 | The posting gate was fiscal-YEAR granular, so "you cannot post into a closed month" could not be expressed; `ledger_entries.fiscal_period_id` was nullable with no FK and `journal_entries` had no period column at all. | Resolved in **S2-07**: `fiscal_periods` exists, `FiscalPeriodCalendarResolver` is bound behind the seam (the posting engine itself is unchanged), `journal_entries.fiscal_period_id` was added and `ledger_entries.fiscal_period_id` backfilled then made `NOT NULL` with a real FK. The posting lock also moved from the fiscal-year row to the period row, so postings into different months no longer serialize. |
| TD-03 | S1-04 | Identity-schema source-of-truth divergence between `AUTH_SERVICE.md` and `MULTI_TENANCY.md` (single identity store vs. multi-region `global_users` mirror; differing `company_users` columns/status enums/404-vs-403). | Resolved — decided and documented in [ADR-0010](docs/architecture/adr/0010-auth-service-authoritative-for-identity-schema.md): AUTH_SERVICE.md is authoritative for the identity/RBAC schema; the multi-region model is deferred and must supersede ADR-0010 if ever adopted (resolved in this commit / ADR-0010). |

# End of Document
